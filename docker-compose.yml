services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: analyst
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: portfolio
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./demo_pageviews.csv:/docker-entrypoint-initdb.d/demo_pageviews.csv
      - ./SQL/SQL_create_table.sql:/docker-entrypoint-initdb.d/01_table.sql
      - ./SQL/SQL_create_index.sql:/docker-entrypoint-initdb.d/02_index.sql
      - ./SQL/SQL_create_views.sql:/docker-entrypoint-initdb.d/03_views.sql

  pgadmin:
    image: dpage/pgadmin4
    container_name: portfolio_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8081:80"
    depends_on:
      - postgres

  grafana:
    image: grafana/grafana-oss
    container_name: portfolio_grafana
    restart: always
    ports:
      - "3001:3000"
    depends_on:
      - postgres
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana_data:/var/lib/grafana

volumes:
  pgdata:
  grafana_data:
